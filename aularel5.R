# Orientação a objetos

#Já sabendo que `mean()` é um genérico

mean.ana <- function(x) {
  print("Ana10")
}

x <- 1:10
class(x) <- "ana"

# Vamos usar o leem

x <- rnorm(36, 100, 50)
x <- new_leem(x, variable = 2)
tabfreq(x)

x 
 